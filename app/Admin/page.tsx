const Admin = () => {
    // useEffect(() => {
    //     fetch("/api/me").then((res) => {
    //         if (!res.ok) router.push("/login");
    //     });
    // }, []);

    return <div>page</div>;
};

export default Admin;
